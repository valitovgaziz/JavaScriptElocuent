let directions = ["ltr", "rtl", "ttb"]


let strLetters = "aaflds"

function MainDairection(str) {
    let arr = []
    for (let i = 0; i < arr.length; i++) {
        console.log(characterScript(str[i]))
    }
}

console.log(characterScript("A"))
MainDairection(strLetters)

function characterScript(code) {
    for (let script of SCRIPTS) {
        
    }
}